# 《Word Caster》队友交接说明

## 环境

- Node.js `>=22.13.0`
- 推荐使用 npm，并以仓库中的 `package-lock.json` 为唯一依赖版本来源。
- 不需要账号、数据库或额外服务器即可本地运行游戏。

## 首次运行

```bash
npm ci
npm run dev
```

正式检查：

```bash
npm run typecheck
npm test
npm run build
npm run package:share
```

`Word-Caster-分享版/index.html` 是已经打包好的离线试玩入口。

## 主要结构

- `app/game/core.ts`：能力、敌人、波次、学习规则和存档数据。
- `app/game/engine.ts`：60Hz游戏逻辑、Canvas渲染、战斗和建造。
- `魔法书/index.html`：保留的480词施法魔典与四步学习流程。
- `incantation-voice-kit`：按住施法、转写、音效、成功环、粒子与震动。
- `scripts/slice-sprites.mjs`：把所有原始分镜统一拆成游戏使用的独立PNG。
- `public/game`：当前构建实际读取的游戏素材。
- `share-src`：离线分享版入口。

## 当前规则

- 前期能力从第1个白昼起开放，后期能力从第4个白昼起开放。
- 每个白昼最多首次学习8个词，复习不占名额，夜晚只能浏览。
- 玩家只与地表碰撞，可以穿过所有己方建筑。
- 场景装饰没有碰撞，不写入存档。
- 新存档键为 `word-caster-save-v2`；首次运行会只读复制旧V2存档，不删除旧数据。
- 魔典课程使用带 `requestId` 的事务消息；连续学习不会再产生状态回传循环。

## 发布说明

`railway.json` 可直接用于 Railway：Node.js `>=22.13.0`，构建
`npm ci && npm run build`，启动 `npm start`，健康检查 `/`。

`.openai/hosting.json` 仅保留现有站点项目标识；交接包不会自动发布或覆盖线上版本。
